<script setup lang="ts">
/**
 * Единственное место с `v-html` во всём проекте: картинки лежат в `.svg`-файлах
 * и приходят строкой (`?raw`), а не ссылкой. Ссылкой нельзя — цвета внутри взяты
 * токенами темы и `currentColor`, а во внешнем файле `<img>` они не вычисляются
 * и картинка не перекрашивается под тёмную тему. Разметка своя, из сборки:
 * чужого содержимого сюда не попадает.
 *
 * Размер задаёт вызывающий — шириной контейнера или инлайновым стилем;
 * высоту картинка берёт из своего `viewBox`.
 */
defineProps<{ markup: string }>()
</script>

<template>
  <!-- eslint-disable vue/no-v-html -- разметка своя, из сборки -->
  <span
    class="inline-flex shrink-0 [&>svg]:h-auto [&>svg]:w-full"
    v-html="markup"
  />
</template>
