# Tasks: add-immersion-mode

## 1. Чистая логика

- [x] 1.1 `utils/immersion.ts`: сопоставление словоформ текста с `translate`
      записей (точное + общий префикс ≥ 4, хвосты ≤ 3); токенизация текста
      по `\p{L}+` — границы слов даёт она, `buildTermsPattern` не подходит
      (строит только точные альтернативы); тест `immersion.test.ts` рядом
      (окончания, слово-в-слове, регистр, пустой словарь)
- [x] 1.2 Отбор кандидатов там же: сортировка due → новые → остальные
      (на `dueEntries`/`dueAt`), лимиты «15 на страницу, 1 на предложение
      (`sentence.ts`), 1 вхождение на запись»; тест на приоритет и лимиты
- [x] 1.3 Определение языка страницы: доля символов алфавита языка перевода
      (`readerSettings.targetLang`) в тексте > 60 %; при совпадающих
      письменностях пары (en → es) — всегда «не язык перевода»; тест на ru/en,
      смешанный текст и пару с общей письменностью

## 2. Настройка и локали

- [x] 2.1 `useReaderSettings`: флаг `immersion` (по умолчанию false)
- [x] 2.2 Переключатель в разделе «чтение» (`ui/options-page/pages/index.vue`)
- [x] 2.3 Строки во все шесть локалей (`src/locales/*`), `locales.test.ts` зелёный

## 3. Вкрапления на странице

- [x] 3.1 Расширить `utils/highlight.ts`: метка с подменным текстом и сохранением
      оригинала в data-атрибуте; восстановление оригинала при снятии меток;
      запрет замены внутри `a`, `pre`, `code`, полей ввода и contenteditable —
      только для замен, не в общем `shouldSkipElement`: обычная подсветка
      сейчас красит слова в ссылках (заголовки reddit), и это поведение
      не трогаем
- [x] 3.2 Развилка в оверлее рядом с вызовом `analyzeText`: включённый режим +
      страница на языке перевода → вкрапления вместо разбора; иначе — текущее
      поведение без изменений
- [x] 3.3 Кнопка режима в `OverlayHeader.vue`; выключение возвращает оригиналы
      без перезагрузки

## 4. Карточка и SRS

- [x] 4.1 `WordCard.vue`: режим вкрапления — оригинальная словоформа из текста,
      данные записи, кнопки «знал / не знал»
- [x] 4.2 Ответ: `updateEntry(entry.id, reviewEntry(entry, isKnown, Date.now()))`,
      как в `training.vue`; после ответа метка раскрывается в оригинал;
      без ответа SRS не меняется

## 5. Финал

- [x] 5.1 `npm run typecheck`, `npm run lint`, `npm test`
- [ ] 5.2 Прогон вживую: русская страница с включённым режимом (вкрапления,
      карточка, самооценка, выключение), английская — обычный разбор
- [x] 5.3 `deferred.md`: упрощение морфологии (префиксное сравнение) с триггером
      «совпадений заметно меньше, чем слов в словаре»; `docs/projectInfo.md` —
      упомянуть режим
